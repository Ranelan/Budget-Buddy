/* Updated Color Variables for Light Blue & White Theme */
:root {
  --bg-main: white;
  --bg-secondary: white;
  --bg-card: white;
  --text-main: #2c3e50;
  --text-secondary: #718096;
  --text-muted: #a0aec0;
  --accent-primary: #4299e1;
  --accent-secondary: #38b2ac;
  --accent-light: #bee3f8;
  --warm-accent: #ed8936;
  --success: #48bb78;
  --danger: #f56565;
  --border: #e2e8f0;
  --shadow: rgba(0, 0, 0, 0.1);
  --shadow-lg: rgba(0, 0, 0, 0.15);
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-hero: linear-gradient(135deg, #4299e1 0%, #38b2ac 100%);
  --btn-bg: white;
  --btn-text: var(--text-main);
  --btn-outline: rgba(255, 255, 255, 0.9);
}

/* Reset and Base Styles */
body, html {
  background: var(--bg-main);
  color: var(--text-main);
  font-family: 'Inter', 'Segoe UI', 'Roboto', Arial, sans-serif;
  margin: 0;
  padding: 0;
  transition: all 0.3s ease;
}

/* Navigation Styles */
.main-nav {
  background: white;
  box-shadow: 0 2px 12px var(--shadow);
  padding: 0.8em 0;
}

.nav-content {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  position: relative;
  min-height: 70px;
}

.nav-logo {
  display: flex;
  align-items: center;
  position: absolute;
  left: 22%;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
}

.nav-links-right {
  display: flex;
  align-items: center;
  gap: 2em;
  list-style: none;
  margin: 0;
  padding: 0;
  position: absolute;
  right: 22%;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
}

.nav-links-right li a {
  color: var(--text-main);
  font-weight: 600;
  text-decoration: none;
  font-size: 1.1em;
  padding: 0.5em 1em;
  border-radius: 8px;
  transition: all 0.2s ease;
}

.nav-links-right li a:hover,
.nav-links-right li a.active {
  background: var(--accent-light);
  color: var(--accent-primary);
}

/* Logo Styles */
.logo-text-link {
  text-decoration: none;
  display: flex;
  align-items: center;
  gap: 0.5em;
}

.logo-text {
  font-size: 1.8rem;
  font-weight: 900;
  color: #4299e1;
  letter-spacing: 0.5px;
  font-family: 'Inter', sans-serif;
}

.logo-text-highlight {
  color: #26c6da;
  margin-left: 0.2em;
}

/* Hero Section */
.hero-bg {
  background: var(--gradient-hero);
  padding: 0;
  margin: 0;
}

.align-with-header {
  display: grid;
  grid-template-columns: 1fr minmax(420px, 480px) 1fr;
  align-items: start;
  margin-top: 2.5em;
  min-height: 400px;
  padding: 4em 2em;
}

.align-with-header .hero-content {
  grid-column: 2;
  min-width: 420px;
  max-width: 480px;
}

.align-with-header .hero-image {
  grid-column: 3;
  margin-left: 2em;
}

.hero-title {
  font-size: 3em;
  font-weight: 900;
  color: white;
  margin-bottom: 0.3em;
  letter-spacing: -0.02em;
  line-height: 1.1;
}

.hero-highlight {
  color: var(--accent-light);
}

.hero-subtitle {
  font-size: 1.3em;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 1.5em;
  line-height: 1.6;
}

.hero-features {
  margin: 2em 0;
  padding-left: 0;
  color: rgba(255, 255, 255, 0.95);
  font-size: 1.1em;
  line-height: 2;
  list-style: none;
}

.hero-features li {
  margin-bottom: 0.8em;
  padding-left: 2em;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 0.8em;
  position: relative;
}

.hero-features li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--accent-light);
  font-weight: bold;
  font-size: 1.2em;
}

.hero-cta-row {
  display: flex;
  gap: 1.5em;
  margin-top: 2em;
}

.hero-btn {
  font-size: 1.1em;
  font-weight: 700;
  border-radius: 12px;
  padding: 1em 2.5em;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  text-decoration: none;
  display: inline-block;
  text-align: center;
}

.hero-btn-gold {
  background: white;
  color: var(--accent-primary);
  box-shadow: 0 4px 15px rgba(255, 255, 255, 0.3);
}

.hero-btn-gold:hover {
  background: var(--accent-light);
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(255, 255, 255, 0.4);
}

.hero-btn-outline {
  background: transparent;
  color: white;
  border: 2px solid rgba(255, 255, 255, 0.8);
}

.hero-btn-outline:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: white;
}

.hero-image {
  font-size: 4em;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 50%;
  width: 120px;
  height: 120px;
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

/* Login Landing Page */
.login-landing-bg {
  min-height: 100vh;
  background: var(--gradient-hero);
  display: flex;
  align-items: center;
  justify-content: center;
}

.login-landing-container {
  background: var(--bg-secondary);
  border-radius: 20px;
  box-shadow: 0 10px 40px var(--shadow-lg);
  padding: 3em 2.5em 2.5em 2.5em;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 380px;
  max-width: 400px;
  border: 1px solid var(--border);
}

.login-landing-logo {
  margin-bottom: 1.5em;
}

.login-landing-form {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 0.7em;
  margin-bottom: 1.2em;
}

.login-label {
  color: var(--accent-primary);
  font-size: 1em;
  font-weight: 700;
  margin-bottom: 0.2em;
}

.login-input {
  background: var(--bg-main);
  border: 2px solid var(--border);
  color: var(--text-main);
  font-size: 1.1em;
  padding: 1em;
  margin-bottom: 1em;
  outline: none;
  border-radius: 12px;
  transition: all 0.2s ease;
  width: 100%;
}

.login-input:focus {
  border-color: var(--accent-primary);
  box-shadow: 0 0 0 3px var(--accent-light);
}

.login-btn {
  margin-top: 1em;
  background: var(--gradient-primary);
  color: white;
  border: none;
  border-radius: 12px;
  font-size: 1.1em;
  font-weight: 700;
  padding: 1em 0;
  cursor: pointer;
  transition: all 0.2s ease;
  width: 100%;
}

.login-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px var(--shadow-lg);
}

.login-dropdown-container {
  width: 100%;
  margin-top: 1.2em;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.login-dropdown {
  width: 100%;
  padding: 1em;
  border-radius: 12px;
  border: 2px solid var(--border);
  background: var(--bg-main);
  color: var(--text-main);
  font-size: 1em;
  font-weight: 600;
  margin-top: 1em;
  outline: none;
  transition: all 0.2s ease;
}

.login-dropdown:focus {
  border-color: var(--accent-primary);
  box-shadow: 0 0 0 3px var(--accent-light);
}
.align-with-header {
  display: grid;
  grid-template-columns: 1fr minmax(420px, 480px) 1fr;
  align-items: start;
  margin-top: 2.5em;
  min-height: 400px;
}

.align-with-header .hero-content {
  grid-column: 2;
  min-width: 420px;
  max-width: 480px;
}

.align-with-header .hero-image {
  grid-column: 3;
  margin-left: 2em;
}

@media (max-width: 900px) {
  .align-with-header {
    grid-template-columns: 1fr;
    margin-left: 0;
    margin-right: 0;
    justify-items: center;
  }
  .align-with-header .hero-content, .align-with-header .hero-image {
    grid-column: 1;
    margin-left: 0;
  }
}
.hero-features {
  margin: 1.5em 0 1em 0;
  padding-left: 1.2em;
  color: var(--text-secondary);
  font-size: 1.1em;
  line-height: 1.7;
  list-style: none;
}
.hero-features li {
  margin-bottom: 0.3em;
  padding-left: 0.2em;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 0.5em;
}
.logo-text-link {
  text-decoration: none;
  display: flex;
  align-items: center;
}

.logo-text {
  font-size: 2rem;
  font-weight: 900;
  color: var(--accent);
  letter-spacing: 1px;
  font-family: 'Montserrat', 'Segoe UI', Arial, sans-serif;
  transition: color 0.2s;
}

.logo-text-highlight {
  color: var(--accent-secondary);
  margin-left: 0.2em;
}
/* Navigation layout for logo left, links right */

.nav-content {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  position: relative;
  min-height: 70px;
}


.nav-logo {
  display: flex;
  align-items: center;
  position: absolute;
  left: 22%;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
}


.nav-links-right {
  display: flex;
  align-items: center;
  gap: 2em;
  list-style: none;
  margin: 0;
  padding: 0;
  position: absolute;
  right: 22%;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
}






/* Footer */
.main-footer {
  background: var(--bg-secondary);
  color: var(--text-secondary);
  border-top: 1px solid var(--border);
  padding: 2em 0 1em 0;
  margin-top: 4em;
}

.footer-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1em;
}

.footer-social {
  display: flex;
  gap: 1.5em;
  margin-top: 0.5em;
}

.footer-social a {
  color: var(--accent-primary);
  transition: all 0.2s ease;
  display: inline-flex;
  align-items: center;
  padding: 0.5em;
  border-radius: 8px;
}

.footer-social a:hover {
  color: var(--warm-accent);
  background: var(--accent-light);
  transform: scale(1.1);
}


/* --- 404 Not Found Page --- */
.notfound-section {
  background: white;
  color: var(--accent-primary);
  border-radius: 18px;
  max-width: 500px;
  margin: 4em auto 2em auto;
  padding: 3em 2em 2.5em 2em;
  box-shadow: 0 4px 24px rgba(33,150,243,0.10);
  text-align: center;
}
.notfound-section h1 {
  font-size: 4em;
  margin-bottom: 0.2em;
  color: #21cbf3;
}
.notfound-section h2 {
  font-size: 2em;
  margin-bottom: 0.7em;
}
.notfound-section p {
  font-size: 1.1em;
  color: var(--text-main);
  margin-bottom: 1.5em;
}
.notfound-home-link {
  display: inline-block;
  background: var(--accent-primary);
  color: white;
  font-weight: 700;
  padding: 0.7em 2em;
  border-radius: 8px;
  text-decoration: none;
  font-size: 1.1em;
  transition: background 0.2s, color 0.2s;
}
.notfound-home-link:hover {
  background: #21cbf3;
  color: white;
}
/* --- Contact Form Validation --- */
.form-error {
  color: #ff4d4f;
  font-size: 0.98em;
  margin-top: -0.7em;
  margin-bottom: 0.7em;
  display: block;
  font-weight: 600;
  letter-spacing: 0.01em;
}
.form-success {
  color: #21cbf3;
  font-size: 1.05em;
  margin-top: 1em;
  display: block;
  font-weight: 600;
  text-align: center;
}
/* Sections */
.about-section,
.contact-section,
.testimonials-section {
  background: var(--bg-card);
  border-radius: 20px;
  max-width: 800px;
  margin: 3em auto;
  padding: 3em 2.5em;
  box-shadow: 0 4px 25px var(--shadow);
  border: 1px solid var(--border);
}

.about-section h2,
.contact-section h2,
.testimonials-section h2 {
  color: var(--accent-primary);
  font-size: 2.5em;
  margin-bottom: 1em;
  text-align: center;
  font-weight: 800;
}

.about-section p,
.contact-section p {
  font-size: 1.2em;
  line-height: 1.8;
  text-align: center;
  color: var(--text-secondary);
}

/* Contact Form */
.contact-form {
  display: flex;
  flex-direction: column;
  gap: 1.5em;
  max-width: 500px;
  margin: 0 auto;
}

.contact-form label {
  color: var(--accent-primary);
  font-weight: 700;
  font-size: 1.1em;
}

.contact-form input,
.contact-form textarea {
  background: var(--bg-main);
  color: var(--text-main);
  border: 2px solid var(--border);
  border-radius: 12px;
  padding: 1em 1.2em;
  font-size: 1em;
  outline: none;
  transition: all 0.3s ease;
  font-family: inherit;
}

.contact-form input:focus,
.contact-form textarea:focus {
  border-color: var(--accent-primary);
  box-shadow: 0 0 0 3px var(--accent-light);
}

.contact-form button {
  background: var(--gradient-primary);
  color: white;
  border: none;
  border-radius: 12px;
  padding: 1.2em 0;
  font-size: 1.2em;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
}

.contact-form button:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px var(--shadow-lg);
}

/* Testimonials */
.testimonials-list {
  display: flex;
  flex-wrap: wrap;
  gap: 2em;
  justify-content: center;
}

.testimonial {
  background: var(--bg-main);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 2em 1.5em;
  max-width: 280px;
  min-width: 220px;
  flex: 1 1 240px;
  transition: all 0.3s ease;
  position: relative;
}

.testimonial:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px var(--shadow-lg);
  border-color: var(--accent-primary);
}

.testimonial::before {
  content: '"';
  font-size: 4em;
  color: var(--accent-light);
  position: absolute;
  top: -10px;
  left: 15px;
  font-family: serif;
}

.testimonial-author {
  display: block;
  color: var(--accent-primary);
  font-weight: 700;
  margin-top: 1.5em;
  font-size: 1em;
}
/* --- Hero Section --- */
.hero-bg {
  background: var(--gradient-hero);
  padding: 0;
  margin: 0;
}
.hero-section {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
  gap: 3em;
  padding: 3em 2em 2em 2em;
  animation: fadeIn 1.2s;
}
.hero-content {
  max-width: 500px;
  display: flex;
  flex-direction: column;
  gap: 1.5em;
}
.hero-title {
  font-size: 2.7em;
  font-weight: 900;
  color: white;
  margin-bottom: 0.2em;
  letter-spacing: 0.01em;
  line-height: 1.1;
}
.hero-highlight {
  color: white;
}
.hero-subtitle {
  font-size: 1.25em;
  color: rgba(255, 255, 255, 0.9);
  margin-bottom: 1em;
}
.hero-cta-row {
  display: flex;
  gap: 1.2em;
}
.hero-btn {
  font-size: 1.1em;
  font-weight: 700;
  border-radius: 8px;
  padding: 0.8em 2em;
  border: none;
  cursor: pointer;
  transition: background 0.2s, color 0.2s, box-shadow 0.2s;
  box-shadow: 0 2px 8px rgba(33,150,243,0.10);
}

.hero-btn-outline {
  background: transparent;
  color: var(--btn-outline);
  border: 2px solid var(--btn-outline);
}
.hero-btn-outline:hover {
  background: var(--btn-outline);
  color: white;
}
.hero-image {
  font-size: 5em;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-secondary);
  border-radius: 50%;
  width: 120px;
  height: 120px;
  box-shadow: 0 4px 24px rgba(33,150,243,0.10);
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(40px); }
  to { opacity: 1; transform: translateY(0); }
}

html {
  scroll-behavior: smooth;
}


main {
  min-height: 70vh;
  padding: 2em 0 3em 0;
}

.main-footer {
  background: white;
  color: var(--text-main);
  border-top: 2px solid #e9ecef;
  padding: 1.2em 0 0.5em 0;
}
.footer-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.7em;
}
.footer-social {
  display: flex;
  gap: 1.2em;
  margin-top: 0.2em;
}
.footer-social a {
  color: #21cbf3;
  transition: color 0.2s, transform 0.2s;
  display: inline-flex;
  align-items: center;
}
.footer-social a:hover {
  color: #ffd700;
  transform: scale(1.15);
}

/* --- About Section --- */
.about-section {
  background: white;
  color: var(--text-main);
  border-radius: 18px;
  max-width: 700px;
  margin: 2em auto;
  padding: 2.5em 2em;
  box-shadow: 0 4px 24px rgba(33,150,243,0.10);
}
.about-section h2 {
  color: #ffd700;
  font-size: 2.2em;
  margin-bottom: 0.7em;
  text-align: center;
}
.about-section p {
  font-size: 1.15em;
  line-height: 1.7;
  text-align: center;
}

/* --- Contact Section --- */
.contact-section {
  background: white;
  color: var(--text-main);
  border-radius: 18px;
  max-width: 700px;
  margin: 2em auto;
  padding: 2.5em 2em;
  box-shadow: 0 4px 24px rgba(33,150,243,0.10);
}
.contact-section h2 {
  color: #ffd700;
  font-size: 2.2em;
  margin-bottom: 0.7em;
  text-align: center;
}
.contact-section p {
  font-size: 1.1em;
  text-align: center;
  margin-bottom: 1.5em;
}
.contact-form {
  display: flex;
  flex-direction: column;
  gap: 1.1em;
  max-width: 400px;
  margin: 0 auto;
}
.contact-form label {
  color: #21cbf3;
  font-weight: 600;
}
.contact-form input,
.contact-form textarea {
  background: white;
  color: var(--text-main);
  border: 1.5px solid #dee2e6;
  border-radius: 8px;
  padding: 0.8em;
  font-size: 1em;
  outline: none;
  transition: border 0.2s, box-shadow 0.2s;
}
.contact-form input:focus,
.contact-form textarea:focus {
  border-color: #21cbf3;
  box-shadow: 0 0 0 2px rgba(33, 203, 243, 0.10);
}
.contact-form button {
  background: linear-gradient(90deg, #2196f3 0%, #21cbf3 100%);
  color: var(--nav-link-active);
  border: none;
  border-radius: 8px;
  padding: 0.9em 0;
  font-size: 1.1em;
  font-weight: 700;
  cursor: pointer;
  transition: background 0.2s, color 0.2s;
}
.contact-form button:hover {
  background: linear-gradient(90deg, #21cbf3 0%, #2196f3 100%);
  color: #ffd700;
}
/* --- Goal Management Page Styles --- */
.goal-page {
  background: white;
  border-radius: 24px;
  box-shadow: 0 8px 32px rgba(33,150,243,0.13);
  padding: 2em 3em;
  max-width: 700px;
  margin: 2em auto;
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: stretch;
}

.goal-page h1 {
  color: var(--accent-secondary);
  font-size: 2em;
  font-weight: 800;
  margin-bottom: 1em;
  text-align: center;
}

.goal-form {
  background: #232a36;
  border-radius: 18px;
  box-shadow: 0 2px 12px rgba(33, 150, 243, 0.10);
  padding: 1.5em 2em;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.2em;
  align-items: center;
  width: 100%;
  max-width: 480px;
  margin: 0 auto 2em auto;
}

.goal-form input {
  background: white;
  color: var(--text-main);
  border: 1.5px solid #dee2e6;
  border-radius: 12px;
  padding: 1em;
  font-size: 1em;
  outline: none;
  transition: border 0.2s, box-shadow 0.2s;
}
.goal-form input:focus {
  border-color: #ffd700;
  box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.15);
}
.goal-form button {
  grid-column: span 2;
  margin-top: 0.5em;
  padding: 1em 0;
  border-radius: 12px;
  font-size: 1em;
  font-weight: 700;
  border: none;
  cursor: pointer;
  background: linear-gradient(90deg, #2196f3 0%, #21cbf3 100%);
  color: #fff;
  transition: background 0.2s, color 0.2s;
}
.goal-form button:hover {
  background: linear-gradient(90deg, #21cbf3 0%, #2196f3 100%);
}

.goal-id-actions {
  display: flex;
  gap: 1em;
  margin: 1.5em auto 2em auto;
  justify-content: center;
}
.goal-id-actions input {
  background: white;
  color: var(--text-main);
  border: 1.5px solid #dee2e6;
  border-radius: 12px;
  padding: 1em;
  font-size: 1em;
  outline: none;
  transition: border 0.2s, box-shadow 0.2s;
}
.goal-id-actions input:focus {
  border-color: #ffd700;
  box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.15);
}
.goal-id-actions button {
  padding: 0.8em 2em;
  border-radius: 12px;
  font-size: 1em;
  font-weight: 700;
  border: none;
  cursor: pointer;
  background: #2196f3;
  color: #fff;
  transition: background 0.2s, color 0.2s;
}
.goal-id-actions button:hover {
  background: #1769aa;
}

.goal-page table {
  width: 100%;
  border-collapse: collapse;
  border-radius: 12px;
  overflow: hidden;
  margin-top: 1em;
  background: #232a36;
  color: #fff;
  box-shadow: 0 2px 12px rgba(33,150,243,0.10);
}
.goal-page th, .goal-page td {
  padding: 1em;
  text-align: left;
}
.goal-page th {
  background: #1a202c;
  font-weight: bold;
  color: #21cbf3;
}
.goal-page tr:nth-child(even) {
  background: #283040;
}
.goal-page tbody tr:hover {
  background: #2196f3;
  color: #fff;
}
/* Home Page Layout Fix */
.signup-content {
  display: flex;
  flex-direction: row;
  gap: 2.5em;
  align-items: flex-start;
  justify-content: space-between;
}
.signup-form-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 320px;
}
.signup-title-group {
  margin-bottom: 2em;
}
.signup-title {
  font-size: 2.4em;
  font-weight: 700;
  margin: 0.2em 0 0.5em 0;
  color: #fff;
}
.signup-subtitle {
  font-size: 1em;
  color: #b0b3b8;
  font-weight: 600;
  letter-spacing: 2px;
  margin-bottom: 0.5em;
}
.signup-login-link {
  font-size: 1.1em;
  color: #b0b3b8;
  margin-bottom: 2em;
  display: block;
}
.signup-btn-row {
  display: flex;
  gap: 2em;
  margin-top: 2em;
  justify-content: flex-start;
}
.signup-btn {
  padding: 0.8em 2em !important;
  border-radius: 14px !important;
  font-size: 1.1em !important;
  font-weight: 700 !important;
  border: none;
  cursor: pointer;
  transition: background 0.2s, color 0.2s, box-shadow 0.2s;
  box-shadow: 0 4px 16px rgba(33,150,243,0.15);
}
.signup-btn-blue {
  background: linear-gradient(90deg, #2196f3 0%, #21cbf3 100%);
  color: #fff;
  letter-spacing: 1px;
}
.signup-btn-blue:hover {
  background: linear-gradient(90deg, #21cbf3 0%, #2196f3 100%);
  box-shadow: 0 8px 24px rgba(33,150,243,0.22);
}
.signup-side-img {
  background: #232a36;
  border-radius: 18px;
  box-shadow: 0 2px 12px rgba(33,150,243,0.10);
  padding: 2em 2em;
  min-width: 260px;
  max-width: 320px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.signup-side-img-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1em;
}
.signup-side-img-icon {
  font-size: 2.5em;
  margin-bottom: 0.5em;
}
.signup-side-img-text {
  color: #fff;
  font-size: 1.1em;
  text-align: center;
}
.signup-footer {
  margin-top: 2em;
  text-align: right;
}
.signup-footer-logo {
  font-weight: 700;
  font-size: 1.2em;
  color: #fff;
}
/* Profile Card Styles */
.profile-card {
  background: #232a36;
  border-radius: 24px;
  box-shadow: 0 8px 32px rgba(33,150,243,0.13);
  padding: 2.5em 3em 2em 3em;
  margin: 2em auto;
  max-width: 400px;
  width: 100%;
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: stretch;
}
.profile-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.5em;
}
.profile-info {
  display: flex;
  flex-direction: column;
  gap: 1em;
  margin-bottom: 2em;
}
.profile-label {
  font-weight: 700;
  color: #21cbf3;
  margin-right: 0.5em;
}
.profile-value {
  color: #ffd700;
}
.profile-form {
  display: flex;
  flex-direction: column;
  gap: 1em;
}
.profile-input {
  background: white;
  border: 1px solid #2196f3;
  border-radius: 10px;
  color: var(--text-main);
  padding: 0.7em 1em;
  font-size: 1em;
  outline: none;
  margin-bottom: 0.5em;
}
.profile-input:focus {
  border-color: #ffd700;
  box-shadow: 0 0 0 2px rgba(255,215,0,0.15);
}
.profile-btn-row {
  display: flex;
  gap: 1em;
  margin-top: 1em;
}
.logout-btn {
  background: #ff5252;
  color: #fff;
  border: none;
  border-radius: 8px;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s;
}
.logout-btn:hover {
  background: #ff1744;
}
/* --- Global & Reusable Component Styles --- */

.enhanced-btn {
  background: linear-gradient(90deg, #2196f3 0%, #21cbf3 100%);
  color: #fff;
  border: none;
  border-radius: 12px;
  padding: 0.8em 2em !important;
  font-size: 1.1em !important;
  font-weight: 700 !important;
  cursor: pointer;
  box-shadow: 0 4px 16px rgba(33, 203, 243, 0.18);
  transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
}

.enhanced-btn:hover, .enhanced-btn:focus {
  background: linear-gradient(90deg, #21cbf3 0%, #2196f3 100%);
  transform: translateY(-3px) scale(1.07);
  box-shadow: 0 8px 24px rgba(33, 203, 243, 0.28);
  outline: none;
}

.btn-edit {
  background: #f39c12; /* Using category page style for consistency */
  color: white;
  border: none;
  border-radius: 8px;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s;
}

.btn-edit:hover {
  background: #e67e22;
}

.btn-delete {
  background: #e74c3c; /* Using category page style for consistency */
  color: white;
  border: none;
  border-radius: 8px;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s;
}

.btn-delete:hover {
  background: #c0392b;
}

/* Base style for inputs, merged from .signup-input */
.signup-input {
  background: white;
  color: var(--text-main);
  border: 1.5px solid #dee2e6;
  border-radius: 12px;
  padding: 1em;
  font-size: 1em;
  outline: none;
  transition: border 0.2s, box-shadow 0.2s;
}

.signup-input:focus {
  border-color: #ffd700; /* Prioritizing this focus color */
  box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.15);
}

.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  align-items: center;
  justify-content: center;
  display: flex;
  padding: 0;
  margin: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  gap: 1em;
  margin: 1.5em auto 2em auto;
  justify-content: center;
  z-index: 1000;
}

.modal-content {
  background: #232a36;
  border-radius: 18px;
  box-shadow: 0 8px 32px rgba(33, 150, 243, 0.18);
  padding: 2em 2.5em;
  min-width: 380px;
  max-width: 480px;
  width: auto;
  max-height: 90vh;
  overflow-y: auto;
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
}


.modal-content h2 {
  margin-bottom: 1em;
  color: #21cbf3;
  font-size: 1.5em;
  font-weight: 700;
}


/* --- Signup/Login Page Styles --- */

.signup-bg {
  min-height: 100vh;
  background: var(--bg-main);
  display: flex;
  align-items: center;
  justify-content: center;
}

.signup-container {
  background: white;
  border-radius: 32px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
  width: 1024px;
  max-width: 98vw;
  padding: 2.5em 2em 2em 2em;
  position: relative;
  color: #fff;
  box-shadow: 0 2px 12px rgba(33,150,243,0.10);
}
.goal-page th, .goal-page td {
  padding: 1em;
  text-align: left;
}

.signup-appname {
  font-weight: 700;
  font-size: 1.2em;
  letter-spacing: 1px;
}

.signup-nav {
  display: flex;
  gap: 2em;
  font-size: 1em;
  color: #b0b3b8;
}

.signup-nav-link {
  cursor: pointer;
  transition: color 0.2s;
}

.signup-nav-link:hover {
  color: #2196f3;
  text-decoration: underline;
}

.signup-content {
  display: flex;
  flex-direction: row;
  gap: 2.5em;
  align-items: flex-start;
  justify-content: space-between;
}

.signup-form-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-width: 320px;
}

.signup-title {
  font-size: 2.4em;
  font-weight: 700;
  margin: 0.2em 0;
}

.signup-login-link {
  font-size: 1em;
  color: #b0b3b8;
}

.signup-link-btn {
  background: none;
  border: none;
  color: #2196f3;
  font-weight: 600;
  cursor: pointer;
  font-size: 1em;
  text-decoration: underline;
}

.signup-form {
  display: flex;
  flex-direction: column;
  gap: 1.2em;
}

.signup-btn {
  padding: 0.8em 2em !important;
  border-radius: 14px !important;
  font-size: 1.1em !important;
  font-weight: 700 !important;
  border: none;
  cursor: pointer;
  transition: background 0.2s, color 0.2s, box-shadow 0.2s;
  box-shadow: 0 4px 16px rgba(33,150,243,0.15);
}

.signup-btn-blue {
  background: linear-gradient(90deg, #2196f3 0%, #21cbf3 100%);
  color: #fff;
  letter-spacing: 1px;
}

.signup-btn-blue:hover {
  background: #1769aa;
}


/* --- Admin Dashboard Styles --- */

.dashboard-bg {
  min-height: 100vh;
  background: var(--bg-main);
  display: flex;
  align-items: center;
  justify-content: center;
}

.dashboard-container {
  background: white;
  border-radius: 32px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  width: 1280px;
  max-width: 98vw;
  min-height: 600px;
  display: flex;
  flex-direction: row;
  color: var(--text-main);
  padding: 2em;
  gap: 2em;
}

/* This is the dashboard-specific sidebar */
.dashboard-sidebar {
  width: 220px;
  background: white;
  border-radius: 24px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.15);
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 2em 1em;
  gap: 1.5em;
}

.dashboard-sidebar-link {
  background: none;
  border: none;
  color: #b0b3b8;
  font-size: 1em;
  font-weight: 600;
  padding: 0.9em 1em;
  margin-bottom: 0.7em;
  display: block;
  box-sizing: border-box;
  min-height: 44px;
  border-radius: 12px;
  cursor: pointer;
  width: 100%; /* Make links full width */
  text-align: left;
  transition: background 0.2s, color 0.2s;
  text-decoration: none;
}

.dashboard-sidebar-link.active,
.dashboard-sidebar-link:hover {
  background: #2196f3;
  color: #fff;
  text-decoration: none;
}

/* This is the dashboard-specific content area */
.dashboard-content {
  flex: 1;
  background: white;
  border-radius: 24px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.10);
  padding: 2em;
  min-height: 400px;
}

.dashboard-table {
  width: 100%;
  border-collapse: collapse;
  border-radius: 8px;
  overflow: hidden;
  margin-bottom: 2em;
}

.dashboard-table th, .dashboard-table td {
  padding: 12px 15px; /* Increased padding */
  text-align: left;
}

.dashboard-table th {
  background: #1a202c;
  font-weight: bold;
}

.dashboard-table tr:nth-child(even) {
  background: #283040;
}

.dashboard-table tbody tr:hover {
  background: #2196f3;
}


/* --- Budget Management Page Styles --- */

.budget-main-container {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  width: 100%;
  padding: 2em 2em 2em 1em;

}

.budget-title {
  color: #fff;
  font-weight: 800;
  font-size: 2.2em;
  letter-spacing: 1px;
  margin-bottom: 2em; /* MODIFICATION: Reinstated margin for spacing */
}

/* Consolidated budget form using Grid */
.budget-form {
  background: #232a36;
  border-radius: 18px;
  box-shadow: 0 2px 12px rgba(33, 150, 243, 0.10);
  padding: 1em 1.2em;
  display: flex;
  flex-direction: column;
  gap: 1.2em;
  align-items: center;
}

.budget-form .modal-btn-row {
  display: flex;
  flex-direction: row;
  gap: 1.2em;
  justify-content: center;
  width: 100%;
  margin-top: 1em;
}

.budgets-title {
  color: #21cbf3;
  font-weight: 700;
  font-size: 1.3em;
  align-self: flex-start;
  margin-bottom: 1.5em; /* MODIFICATION: Reinstated margin for spacing */
}

.budget-cards-container {
  width: 100%;
  display: flex;
  flex-wrap: wrap;
  gap: 2em;
  justify-content: flex-start;
  max-width: 1200px;
}

/* Consolidated budget card with enhanced styles */
.budget-card {
  background: linear-gradient(135deg, #232a36 70%, #263043 100%);
  border-radius: 18px;
  box-shadow: 0 4px 24px rgba(33, 150, 243, 0.13);
  padding: 1.5em 2em;
  min-width: 220px;
  max-width: 260px;
  min-height: 150px; /* Added min-height to ensure consistent card size */
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  cursor: pointer;
  transition: box-shadow 0.2s, transform 0.2s, border-color 0.2s;
  margin-bottom: 1em;
  border: 1px solid #232a36;
}

.budget-card:hover {
  box-shadow: 0 8px 32px rgba(33, 150, 243, 0.22);
  transform: translateY(-4px) scale(1.04);
  border-color: #2196f3;
}

.budget-card-month-year {
  color: #21cbf3;
  font-weight: 700;
  font-size: 1.2em;
  margin-bottom: 0.5em;
}

.budget-card-limit-amount {
  color: #ffd700;
  font-weight: 700;
  font-size: 1.3em;
}

/* Container for the action buttons */
.budget-card-actions {
  display: flex;
  gap: 1em;
  width: 100%;
  justify-content: flex-end; /* Aligns buttons to the right */
  margin-top: 1em;
  
  /* Hide by default */
  opacity: 0;
  visibility: hidden;
  transform: translateY(10px); /* Start slightly lower */
  transition: opacity 0.3s ease, transform 0.3s ease, visibility 0s 0.3s;
}

/* Show buttons when the card has the .active class */
.budget-card.active .budget-card-actions {
  opacity: 1;
  visibility: visible;
  transform: translateY(0); /* Move to original position */
  transition: opacity 0.3s ease, transform 0.3s ease, visibility 0s 0s;
}

.no-budgets {
  color: #b0b3b8;
  font-size: 1.1em;
}

/* --- Landing Page & App Styles --- */

.App {
  text-align: center;
  background: linear-gradient(135deg, #0a2342 0%, #193a6a 100%);
  min-height: 100vh;
}

.App-header {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: calc(10px + 2vmin);
  color: white;
}

.Landing-title {
  font-size: 3rem;
  font-weight: 700;
  color: #008080;
  margin-bottom: 0.5em;
}

.Landing-link {
  color: #fff;
  background: #444c56;
  padding: 0.75em 2em;
  border-radius: 30px;
  text-decoration: none;
  font-weight: 600;
  transition: background 0.2s;
}

.Landing-link:hover {
  background: #b0b3b8;
  color: #185a9d;
}


/* User Dashboard Categories Styles */
.dashboard-content .category-management {
  padding: 0;
  margin: 0;
  max-width: none;
  background: transparent;
}

.dashboard-content .category-header {
  margin-bottom: 30px;
  padding: 0;
  background: none;
  box-shadow: none;
  text-align: center;
}

.dashboard-content .category-header h1 {
  font-size: 2rem;
  margin-bottom: 10px;
  color: #2196f3;
}

.dashboard-content .category-header p {
  color: #b0b3b8;
  font-size: 1.1rem;
}

.dashboard-content .user-search {
  display: flex;
  gap: 15px;
  margin-bottom: 25px;
  align-items: center;
  background: #232a36;
  padding: 15px;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.dashboard-content .user-search input {
  flex: 1;
  padding: 12px 16px;
  border: 2px solid #3a4151;
  border-radius: 8px;
  background: #1a1f2d;
  color: white;
  font-size: 14px;
  transition: all 0.3s ease;
}

.dashboard-content .user-search input:focus {
  outline: none;
  border-color: #2196f3;
  box-shadow: 0 0 0 3px rgba(33, 150, 243, 0.1);
}

.dashboard-content .user-search input::placeholder {
  color: #6c757d;
}

.dashboard-content .category-table {
  background: #232a36;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.dashboard-content .category-table th {
  background: #2c3344;
  padding: 15px;
  color: #2196f3;
  font-weight: 600;
  border-bottom: 2px solid #3a4151;
}

.dashboard-content .category-table td {
  padding: 15px;
  color: white;
  border-bottom: 1px solid #3a4151;
}

.dashboard-content .category-table tr:hover {
  background: #2c3344;
}

.dashboard-content .type-badge {
  padding: 6px 12px;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 500;
  text-transform: uppercase;
}

.dashboard-content .type-badge.expense {
  background: linear-gradient(135deg, #ff6b6b, #ee5a52);
  color: white;
}

.dashboard-content .type-badge.income {
  background: linear-gradient(135deg, #51cf66, #40c057);
  color: white;
}

.dashboard-content .btn-edit {
  background: #17a2b8;
  color: white;
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  margin-right: 8px;
  transition: all 0.3s ease;
}

.dashboard-content .btn-edit:hover {
  background: #138496;
  transform: translateY(-1px);
}

.dashboard-content .btn-delete {
  background: #dc3545;
  color: white;
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.dashboard-content .btn-delete:hover {
  background: #c82333;
  transform: translateY(-1px);
}

/* Modal styles for dashboard */
.dashboard-content .modal-overlay {
  background: rgba(0, 0, 0, 0.8);
}

.dashboard-content .category-form {
  background: #232a36;
  border: 1px solid #3a4151;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
}

.dashboard-content .category-form h2 {
  color: #2196f3;
}

.dashboard-content .form-group label {
  color: white;
}

.dashboard-content .form-group input,
.dashboard-content .form-group select {
  background: #1a1f2d;
  border: 2px solid #3a4151;
  color: white;
}

.dashboard-content .form-group input:focus,
.dashboard-content .form-group select:focus {
  border-color: #2196f3;
  box-shadow: 0 0 0 3px rgba(33, 150, 243, 0.1);
}

/* Responsive design */
@media (max-width: 768px) {
  .dashboard-content .user-search {
    flex-direction: column;
    align-items: stretch;
  }
  
  .dashboard-content .category-table {
    font-size: 0.9rem;
  }
  
  .dashboard-content .category-table th,
  .dashboard-content .category-table td {
    padding: 10px;
  }
  
  .dashboard-content .btn-edit,
  .dashboard-content .btn-delete {
    padding: 6px 12px;
    font-size: 0.8rem;
  }
}


/*Category Style*/
.category-management {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
  font-family: 'Arial', sans-serif;
  background-color: var(--bg-main);
  color: var(--text-main);
  min-height: 100vh;
}

/* Header styles */
.category-header {
  text-align: center;
  margin-bottom: 30px;
}

.category-header h1 {
  font-size: 2rem;
  font-weight: 600;
  color: white;
  margin: 0;
}

.category-header p {
  font-size: 1rem;
  color: #b0b0b0;
  margin: 5px 0 0;
}

/* Admin filters section */
.admin-filters {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  margin-bottom: 20px;
  justify-content: space-between;
  align-items: center;
}

.filter-group {
  flex: 1;
  min-width: 200px;
}

.filter-group input,
.filter-group select {
  width: 100%;
  padding: 10px;
  border: 1px solid #3a3f50;
  border-radius: 5px;
  font-size: 1rem;
  background-color: #2c3040;
  color: #e0e0e0;
  box-sizing: border-box;
}

.filter-group input:focus,
.filter-group select:focus {
  outline: none;
  border-color: #4a90e2;
  box-shadow: 0 0 5px rgba(74, 144, 226, 0.3);
}

/* User search section */
.user-search {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  margin-bottom: 20px;
  align-items: center;
}

.user-search input {
  flex: 1;
  padding: 10px;
  border: 1px solid #3a3f50;
  border-radius: 5px;
  font-size: 1rem;
  background-color: #2c3040;
  color: #e0e0e0;
}

.user-search input:focus {
  outline: none;
  border-color: #4a90e2;
  box-shadow: 0 0 5px rgba(74, 144, 226, 0.3);
}

/* Buttons */
.btn-primary,
.btn-secondary,
.btn-edit,
.btn-delete {
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-size: 1rem;
  transition: background-color 0.2s ease, transform 0.1s ease;
  min-width: 100px;
  text-align: center;
}

.btn-primary {
  background-color: #4a90e2;
  color: white;
}

.btn-primary:hover {
  background-color: #357abd;
  transform: scale(1.05);
}

.btn-secondary {
  background-color: #5f6368;
  color: white;
}

.btn-secondary:hover {
  background-color: #4b4f54;
  transform: scale(1.05);
}

.btn-edit {
  background-color: #2ecc71;
  color: white;
  margin-right: 10px;
}

.btn-edit:hover {
  background-color: #27ae60;
  transform: scale(1.05);
}

.btn-delete {
  background-color: #e74c3c;
  color: white;
}

.btn-delete:hover {
  background-color: #c0392b;
  transform: scale(1.05);
}

/* Category table */
.category-table {
  width: 100%;
  border-collapse: collapse;
  background-color: #2c3040;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
}

.category-table th,
.category-table td {
  padding: 12px;
  text-align: left;
  border-bottom: 1px solid #3a3f50;
}

.category-table th {
  background-color: #1b1e2a;
  color: white;
  font-weight: 600;
}

.system-category {
  background-color: #3a3f50;
}

.user-category {
  background-color: #2c3040;
}

.type-badge {
  display: inline-flex;
  align-items: center;
  padding: 5px 10px;
  border-radius: 12px;
  font-size: 0.9rem;
}

.type-badge.expense {
  background-color: #4a2c2c;
  color: #ff9999;
}

.type-badge.income {
  background-color: #2c4a2c;
  color: #99ff99;
}

.badge-global {
  margin-left: 8px;
  background-color: #0288d1;
  color: white;
  padding: 2px 8px;
  border-radius: 10px;
  font-size: 0.8rem;
}

/* Modal overlay */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

/* Category form */
.category-form {
  background: #2c3040;
  padding: 20px;
  border-radius: 8px;
  width: 100%;
  max-width: 500px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

.category-form h2 {
  margin-top: 0;
  color: white;
}

.form-group {
  margin-bottom: 15px;
}

.form-group label {
  display: block;
  margin-bottom: 5px;
  font-weight: 600;
  color: #e0e0e0;
}

.form-group input,
.form-group select {
  width: 100%;
  padding: 10px;
  border: 1px solid #3a3f50;
  border-radius: 5px;
  font-size: 1rem;
  background-color: #1b1e2a;
  color: #e0e0e0;
}

.form-group input.error,
.form-group select.error {
  border-color: #e74c3c;
}

.error-text {
  color: #e74c3c;
  font-size: 0.85rem;
  margin-top: 5px;
  display: block;
}

.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 20px;
}

/* Reassign dialog */
.reassign-dialog {
  background: #2c3040;
  padding: 20px;
  border-radius: 8px;
  width: 100%;
  max-width: 500px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
}

.reassign-dialog h2 {
  margin-top: 0;
  color: white;
}

/* Responsive design */
@media (max-width: 768px) {
  .admin-filters,
  .user-search {
    flex-direction: column;
    align-items: stretch;
  }

  .filter-group {
    min-width: unset;
  }

  .category-table th,
  .category-table td {
    font-size: 0.9rem;
    padding: 8px;
  }

  .category-form,
  .reassign-dialog {
    max-width: 90%;
  }

  .btn-primary,
  .btn-secondary,
  .btn-edit,
  .btn-delete {
    padding: 8px 16px;
    font-size: 0.9rem;
  }
}

@media (max-width: 480px) {
  .category-header h1 {
    font-size: 1.5rem;
  }

  .category-table {
    display: block;
    overflow-x: auto;
  }

  .form-actions {
    flex-direction: column;
    gap: 10px;
  }

  .form-actions button {
    width: 100%;
  }
}

/*Recurring Transaction style*/
.recurring-dashboard {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.dashboard-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  padding: 20px;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.dashboard-header h1 {
  color: #2c3e50;
  font-size: 2rem;
}

.error-message {
  background: #ffebee;
  color: #c62828;
  padding: 10px 15px;
  border-radius: 6px;
  border: 1px solid #ef5350;
}

.transactions-table {
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  overflow: hidden;
  margin-bottom: 20px;
}

/* Add these styles to your App.css */
.loading-small {
  font-size: 0.8rem;
  color: #7f8c8d;
  margin-top: 5px;
}

.error-close {
  background: none;
  border: none;
  color: #c62828;
  cursor: pointer;
  font-size: 1.2rem;
  margin-left: 10px;
}

.error-close:hover {
  color: #b71c1c;
}

.transaction-count {
  margin-left: 15px;
  color: #7f8c8d;
  font-size: 0.9rem;
}

.btn-add:disabled {
  background: #95a5a6;
  cursor: not-allowed;
}

.btn-add:disabled:hover {
  transform: none;
  box-shadow: none;
}

.btn-save:disabled {
  background: #95a5a6;
  cursor: not-allowed;
}

.btn-save:disabled:hover {
  background: #95a5a6;
}

/* Debug and Error Styles */
.btn-debug {
  background: #ff6b6b;
  color: white;
  padding: 8px 16px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9rem;
  margin-bottom: 10px;
}

.btn-debug:hover {
  background: #ee5a52;
}

.error-message {
  background: #ffebee;
  color: #c62828;
  padding: 12px 16px;
  border-radius: 6px;
  border: 1px solid #ef5350;
  margin-bottom: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.error-message strong {
  font-weight: 600;
}

.text-link {
  background: none;
  border: none;
  color: #3498db;
  cursor: pointer;
  text-decoration: underline;
  padding: 0;
  font: inherit;
}

.text-link:hover {
  color: #2980b9;
}

/* Console debug info */
.debug-info {
  background: #2c3e50;
  color: white;
  padding: 15px;
  border-radius: 6px;
  margin: 10px 0;
  font-family: monospace;
  font-size: 0.9rem;
}


/* Recurring Transactions Styles */
.transaction-description {
  font-weight: 500;
  color: #e0e0e0;
}

.transaction-amount {
  font-weight: bold;
  color: #4caf50;
}

.transaction-category {
  background: rgba(76, 175, 80, 0.1);
  color: #4caf50;
  padding: 4px 8px;
  border-radius: 12px;
  font-size: 12px;
}

.next-execution {
  color: #bb86fc;
  font-weight: 500;
}

.transaction-summary {
  background: #2c3e50;
  padding: 15px;
  border-radius: 8px;
  margin-top: 20px;
  border-left: 4px solid #21cbf3;
}

.transaction-summary h3 {
  margin: 0 0 10px 0;
  color: #21cbf3;
}

.transaction-summary p {
  margin: 0;
  color: #b0b3b8;
}

.text-link {
  background: none;
  border: none;
  color: #21cbf3;
  text-decoration: underline;
  cursor: pointer;
  font-size: inherit;
}

.text-link:hover {
  color: #03a9f4;
}

.success-message {
  background-color: #d4edda;
  color: #155724;
  padding: 12px;
  border-radius: 6px;
  margin: 10px 0;
  border-left: 4px solid #28a745;
}

.success-message button {
  background: none;
  border: none;
  color: #155724;
  float: right;
  cursor: pointer;
  font-size: 18px;
}




/* Dynamic grid columns based on admin/user role */
.table-header {
  display: grid;
  grid-template-columns: 1fr 1.5fr 1.5fr 1fr 2fr;
  gap: 15px;
  padding: 15px 20px;
  background: #2c3e50;
  color: white;
  font-weight: 600;
}

.table-header.admin-view {
  grid-template-columns: 1fr 1.5fr 1.5fr 1.5fr 1fr 2fr;
}

.table-row {
  display: grid;
  grid-template-columns: 1fr 1.5fr 1.5fr 1fr 2fr;
  gap: 15px;
  padding: 15px 20px;
  border-bottom: 1px solid #eee;
  align-items: center;
}

.table-row.admin-view {
  grid-template-columns: 1fr 1.5fr 1.5fr 1.5fr 1fr 2fr;
}

.table-row:hover {
  background: #f8f9fa;
}

.user-name {
  font-weight: 500;
  color: #7e57c2;
}

.recurrence-type {
  padding: 4px 8px;
  border-radius: 12px;
  font-size: 0.9rem;
  font-weight: 500;
  text-align: center;
  background: #e3f2fd;
  color: #1976d2;
}

.days-left {
  font-weight: 600;
}

.days-left.urgent {
  color: #e74c3c;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% { opacity: 1; }
  50% { opacity: 0.5; }
  100% { opacity: 1; }
}

.actions {
  display: flex;
  gap: 8px;
}

.btn-edit, .btn-delete, .btn-add {
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9rem;
  transition: all 0.3s ease;
}

.btn-edit {
  background: #3498db;
  color: white;
}

.btn-edit:hover {
  background: #2980b9;
}

.btn-delete {
  background: #e74c3c;
  color: white;
}

.btn-delete:hover {
  background: #c0392b;
}

.btn-add {
  background: #9b59b6;
  color: white;
  padding: 12px 24px;
  font-size: 1rem;
  font-weight: 600;
}

.btn-add:hover {
  background: #8e44ad;
  transform: translateY(-2px);
}

.loading, .no-data {
  text-align: center;
  padding: 40px;
  color: #7f8c8d;
  font-style: italic;
}

/* Modal Styles */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.transaction-form {
  background: white;
  padding: 30px;
  border-radius: 12px;
  width: 90%;
  max-width: 450px;
}

.transaction-form h2 {
  color: #2c3e50;
  margin-bottom: 20px;
  text-align: center;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 5px;
  font-weight: 600;
  color: #2c3e50;
}

.form-group input,
.form-group select {
  width: 100%;
  padding: 12px;
  border: 2px solid #ddd;
  border-radius: 6px;
  font-size: 1rem;
  transition: border-color 0.3s ease;
}

.form-group input:focus,
.form-group select:focus {
  outline: none;
  border-color: #3498db;
}

.form-actions {
  display: flex;
  gap: 15px;
  justify-content: flex-end;
  margin-top: 30px;
}

.btn-cancel, .btn-save {
  padding: 12px 24px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 600;
  transition: all 0.3s ease;
}

.btn-cancel {
  background: #95a5a6;
  color: white;
}

.btn-cancel:hover {
  background: #7f8c8d;
}

.btn-save {
  background: #27ae60;
  color: white;
}

.btn-save:hover {
  background: #229954;
}

/* Responsive Design */
@media (max-width: 768px) {
  .table-header,
  .table-row {
    grid-template-columns: 1fr;
    gap: 8px;
  }
  
  .table-header.admin-view,
  .table-row.admin-view {
    grid-template-columns: 1fr;
  }
  
  .table-header {
    display: none;
  }
  
  .table-row {
    padding: 15px;
    border: 1px solid #eee;
    border-radius: 8px;
    margin-bottom: 10px;
  }
  
  .table-row span::before {
    content: attr(data-label);
    font-weight: 600;
    margin-right: 8px;
    color: #7f8c8d;
  }
  
  .actions {
    justify-content: center;
  }
  
  .transaction-form {
    padding: 20px;
    margin: 10px;
  }
}

.transactions-table {
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  overflow: hidden;
  margin-bottom: 20px;
}

.table-header {
  display: grid;
  grid-template-columns: 1fr 1.5fr 1.5fr 1.5fr 1fr 2fr;
  gap: 15px;
  padding: 15px 20px;
  background: #2c3e50;
  color: white;
  font-weight: 600;
}

.table-row {
  display: grid;
  grid-template-columns: 1fr 1.5fr 1.5fr 1.5fr 1fr 2fr;
  gap: 15px;
  padding: 15px 20px;
  border-bottom: 1px solid #eee;
  align-items: center;
}

.table-row:hover {
  background: #f8f9fa;
}

.user-name {
  font-weight: 500;
  color: #7e57c2;
}

.recurrence-type {
  padding: 4px 8px;
  border-radius: 12px;
  font-size: 0.9rem;
  font-weight: 500;
  text-align: center;
  background: #e3f2fd;
  color: #1976d2;
}

.days-left {
  font-weight: 600;
}

.days-left.urgent {
  color: #e74c3c;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% { opacity: 1; }
  50% { opacity: 0.5; }
  100% { opacity: 1; }
}

.actions {
  display: flex;
  gap: 8px;
}

.btn-delete {
  background: #e74c3c;
  color: white;
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9rem;
  transition: all 0.3s ease;
}

.btn-delete:hover {
  background: #c0392b;
}

.btn-refresh {
  background: #3498db;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 1rem;
}

.btn-refresh:hover {
  background: #2980b9;
}

.no-data {
  text-align: center;
  padding: 40px;
  color: #7f8c8d;
  font-style: italic;
}

/* Additional Responsive Design and Utility Classes */
@media (max-width: 900px) {
  .align-with-header {
    grid-template-columns: 1fr;
    margin-left: 0;
    margin-right: 0;
    justify-items: center;
    padding: 2em 1em;
  }
  
  .align-with-header .hero-content, 
  .align-with-header .hero-image {
    grid-column: 1;
    margin-left: 0;
  }
  
  .hero-title {
    font-size: 2.5em;
  }
  
  .nav-logo,
  .nav-links-right {
    position: static;
    transform: none;
  }
  
  .nav-content {
    flex-direction: column;
    gap: 1em;
  }
}

@media (max-width: 768px) {
  .table-header,
  .table-row {
    grid-template-columns: 1fr;
    gap: 8px;
  }
  
  .table-header {
    display: none;
  }
  
  .table-row {
    padding: 15px;
    border: 1px solid #eee;
    border-radius: 8px;
    margin-bottom: 10px;
  }
  
  .table-row span::before {
    content: attr(data-label);
    font-weight: 600;
    margin-right: 8px;
    color: #7f8c8d;
  }
  
  .actions {
    justify-content: center;
  }
}

@media (max-width: 600px) {
  .hero-cta-row {
    flex-direction: column;
    gap: 1em;
  }
  
  .hero-btn {
    width: 100%;
  }
  
  .testimonials-list {
    flex-direction: column;
  }
  
  .about-section,
  .contact-section,
  .testimonials-section {
    margin: 2em 1em;
    padding: 2em 1.5em;
  }
}

/* Form Validation */
.form-error {
  color: var(--danger);
  font-size: 0.9em;
  margin-top: 0.5em;
  font-weight: 600;
}

.form-success {
  color: var(--success);
  font-size: 1em;
  margin-top: 1em;
  font-weight: 600;
  text-align: center;
}

/* Utility Classes */
.text-center { text-align: center; }
.mb-1 { margin-bottom: 1em; }
.mb-2 { margin-bottom: 2em; }
.mt-1 { margin-top: 1em; }
.mt-2 { margin-top: 2em; }

/* Enhanced Button Styles */
.enhanced-btn {
  background: var(--gradient-primary);
  color: white;
  border: none;
  border-radius: 12px;
  padding: 1em 2em;
  font-size: 1.1em;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  text-decoration: none;
  display: inline-block;
}

.enhanced-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px var(--shadow-lg);
}
